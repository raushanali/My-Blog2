<%- include('partials/header') %>

<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4 text-primary">
                <i class="fas fa-newspaper me-3"></i>Latest Blog Posts
            </h1>
            <a href="/create" class="btn btn-primary btn-lg">
                <i class="fas fa-plus me-2"></i>Create New Post
            </a>
        </div>
    </div>
</div>

<% if (posts.length === 0) { %>
    <div class="row">
        <div class="col-12">
            <div class="text-center py-5">
                <i class="fas fa-file-alt fa-5x text-muted mb-4"></i>
                <h3 class="text-muted mb-3">No posts yet!</h3>
                <p class="text-muted mb-4">Start sharing your thoughts by creating your first blog post.</p>
                <a href="/create" class="btn btn-primary btn-lg">
                    <i class="fas fa-plus me-2"></i>Create Your First Post
                </a>
            </div>
        </div>
    </div>
<% } else { %>
    <div class="row">
        <% posts.forEach(post => { %>
            <div class="col-lg-6 col-md-12 mb-4">
                <article class="card h-100 shadow-sm blog-card">
                    <div class="card-body">
                        <h2 class="card-title h4 mb-3">
                            <a href="/posts/<%= post.id %>" class="text-decoration-none text-dark">
                                <%= post.title %>
                            </a>
                        </h2>
                        
                        <div class="post-meta mb-3">
                            <small class="text-muted">
                                <i class="fas fa-user me-1"></i>By <%= post.author %>
                                <span class="mx-2">•</span>
                                <i class="fas fa-calendar me-1"></i><%= formatDate(post.createdAt) %>
                            </small>
                        </div>
                        
                        <p class="card-text text-muted">
                            <%= post.content.length > 150 ? post.content.substring(0, 150) + '...' : post.content %>
                        </p>
                    </div>
                    
                    <div class="card-footer bg-transparent">
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="/posts/<%= post.id %>" class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-eye me-1"></i>Read More
                            </a>
                            
                            <div class="btn-group" role="group">
                                <a href="/posts/<%= post.id %>/edit" class="btn btn-outline-secondary btn-sm" title="Edit Post">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form method="POST" action="/posts/<%= post.id %>?_method=DELETE" style="display: inline;" 
                                      onsubmit="return confirm('Are you sure you want to delete this post?')">
                                    <button type="submit" class="btn btn-outline-danger btn-sm" title="Delete Post">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </article>
            </div>
        <% }); %>
    </div>
<% } %>

<!-- Statistics Section -->
<div class="row mt-5">
    <div class="col-12">
        <div class="alert alert-light">
            <div class="row text-center">
                <div class="col-md-4">
                    <i class="fas fa-file-alt fa-2x text-primary mb-2"></i>
                    <h5 class="mb-0"><%= posts.length %></h5>
                    <small class="text-muted">Total Posts</small>
                </div>
                <div class="col-md-4">
                    <i class="fas fa-users fa-2x text-success mb-2"></i>
                    <h5 class="mb-0"><%= [...new Set(posts.map(p => p.author))].length %></h5>
                    <small class="text-muted">Authors</small>
                </div>
                <div class="col-md-4">
                    <i class="fas fa-calendar fa-2x text-info mb-2"></i>
                    <h5 class="mb-0">
                        <% if (posts.length > 0) { %>
                            <%= formatDate(Math.max(...posts.map(p => new Date(p.createdAt)))) %>
                        <% } else { %>
                            -
                        <% } %>
                    </h5>
                    <small class="text-muted">Latest Post</small>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>